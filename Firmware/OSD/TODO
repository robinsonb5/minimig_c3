| Menu overhaul - nearly complete

Joystick autofire!

Action Replay - helptext about freeze button once it works.

Change write-protect status of ADFs

Create blank ADFs	    \  Investigate FAT writing bugs?
Create blank hardfiles  /

=> Requires Line editor for filenames?

Once we have a line editor, allow named configs.

DMS format?  Kryoflux format?
(DMS isn't going to be possible because it maintains cruncher context over multiple tracks => no random access.

| Need a Buffered file structure capable of random access; currently can only do block-level access.

Allow boot process to be paused, which would allow settings to changed before the Minimig boots.

